export const metadata = {
  title: "LMS Portal | Gryffindors",
  description: "Access your courses, track progress, and earn certificates.",
};

export default function LMSLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
